spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: main-service
          uri: lb://main-service
          predicates:
            - Path=/**

logging:
  level:
    root: info
    org.springframework.cloud.gateway: DEBUG
