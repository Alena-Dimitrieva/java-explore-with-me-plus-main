$kind: http-request
url: "{{baseUrl}}/users/:userId/events/:eventId/likes"
method: DELETE
headers:
  Accept: application/json
pathVariables:
  - key: userId
    value: "{{uid}}"
    description: (Required) id текущего пользователя
  - key: eventId
    value: "{{eid}}"
    description: (Required) id текущего события
scripts:
  - type: beforeRequest
    code: "const main = async () => {\r

      \    const api = new API(pm);\r

      \    const rnd = new RandomUtils();\r

      \r

      \    try {\r

      \        const initiator = await api.addUser(rnd.getUser());\r

      \r

      \        const category = await api.addCategory(rnd.getCategory());\r

      \r

      \        let event = await api.addEvent(initiator.id, rnd.getEvent(category.id));\r

      \        pm.collectionVariables.set(\"eid\", event.id);\r

      \r

      \        event = await api.publishEvent(event.id);\r

      \r

      \        const user = await api.addUser(rnd.getUser());\r

      \        pm.collectionVariables.set('uid', user.id);\r

      \r

      \        await api.addLike(user.id, event.id);\r

      \        const likedEvent = await api.findEvent(event.id);\r

      \        if (likedEvent.rate !== 1) {\r

      \            throw \"Ошибка: Ожидается, что лайк увеличит рейтинг события до 1, но фактически рейтинг не изменился.\"\r

      \        }\r

      \r

      \    } catch (err) {\r

      \        console.error(\"Ошибка при подготовке тестовых данных.\", err);\r

      \    }\r

      };\r

      \r

      const interval = setInterval(() => { }, 1000);\r

      \r

      setTimeout(async () => {\r

      \    try {\r

      \        await main();\r

      \    } catch (e) {\r

      \        console.error(e);\r

      \    } finally {\r

      \        clearInterval(interval);\r

      \    }\r

      },\r

      \    100\r

      );"
    language: text/javascript
  - type: afterResponse
    code: "pm.test(\"Ответ должен содержать код статуса 200 и данные в формате json\", function () {\r

      \    pm.response.to.be.success;\r

      });\r

      \r

      \r

      \r

      \r\n"
    language: text/javascript
order: 5000
